@model IEnumerable<MagisterWeb.Models.Professor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MatProf)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomProf)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MatProf)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomProf)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdtProf }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdtProf }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdtProf })
        </td>
    </tr>
}

</table>
